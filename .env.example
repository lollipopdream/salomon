# OpenAI API Key — required for AI advice generation
# Without this, the app uses a rule-based fallback
VITE_OPENAI_API_KEY=sk-...

# Weather API URL (default: Open-Meteo free tier, no key needed)
VITE_WEATHER_API_URL=https://api.open-meteo.com/v1/forecast
